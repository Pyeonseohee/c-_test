#include <vector>
#include <queue>

using namespace std;

int solution(vector<int> priorities, int location) {
    int answer = 0;

    
    return answer;
}

int main(void){
    vector<int> p = {2, 1, 3, 2};
    vector<int> p2 = {9, 1, 1, 1, 1, 1};
    int l = 2;
    //solution(p, l);
    solution(p2, l);

    return 0;
}